#Placement of .env matters
  ->this is dependent on the process and doesnt care about the file strucutres.
  ->the process starts at the folder where it was run from ,so .env should be placed at 
    the same level as the process is run from.
